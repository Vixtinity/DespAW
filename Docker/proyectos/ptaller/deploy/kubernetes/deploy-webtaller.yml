apiVersion: apps/v1
kind: Deployment
metadata:
 name: webtaller-deployment
 namespace: ptaller
spec:
 replicas: 3
 selector:
   matchLabels:
     app: webtaller
 template:
   metadata:
     labels:
       app: webtaller
   spec:
     containers:
       - name: webtaller
         image: iferlop/webtaller:latest
         # imagePullPolicy: Never
         ports:
           - containerPort: 80
